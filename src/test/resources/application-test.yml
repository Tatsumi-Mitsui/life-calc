# ===== TEST =====
spring:
    datasource:
        url: jdbc:h2:mem:life_calc_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
        driver-class-name: org.h2.Driver
        username: sa
        password: ""
    
    jpa:
        hibernate:
            ddl-auto: create-drop   # テストごとに作って削除
        database-platform: org.hibernate.dialect.H2Dialect
        open-in-view: false
    
    flyway:
        enabled: false